# JPTC Collect · 软件使用指南

**怎么拍**看 [拍摄指南.md](拍摄指南.md)，这份只讲软件怎么用。

软件全程本地运行、不联网、只读你的 RAW。它算完统计量写出几个 CSV 打成一个 zip，你寄回那个 zip 就行，照片不离开你的电脑。

---

## 安装

双击 `JPTC Collect Setup 0.1.0.exe`，一路下一步。Windows 64 位。没有代码签名，SmartScreen 拦的话点「更多信息」→「仍要运行」。

---

## 一、填拍摄模式

硬门槛，填完之前下面三个入口点不动。里面每一项都是从文件里读不出来、事后也补不回来的。

![填完的拍摄模式](docs/images/02-mode.png)

| 字段 | 填什么 |
|---|---|
| **JPEG 输出像素** | 这台机器拍出来的 JPEG 的宽和高。见下面的框 |
| 快门类型 | 相机菜单里叫什么就写什么：机械快门 / 电子前帘 / 电子快门 |
| 压缩 | 无损 / 无压缩 / 有损。有损也能测，但要如实填 |
| 固件版本 | 选填。同机身不同固件的读出可以完全不同 |
| 镜头 | 拍黑场填「无镜头，机身盖」；装着镜头就填型号 |
| 长曝光降噪 + 防抖 | 一个勾，确认两个都关了 |

> **JPEG 输出像素填错，整条曲线会差一个比例，而曲线本身看不出任何毛病，事后也查不出来。**
>
> 所有机型都归一到 2160 高的输出，这个高度就是那把尺子。软件不替你从 RAW 读——RAW 有遮蔽区和机身裁切，两者不一样。**对着你自己拍的 JPEG 确认一遍。**

---

## 二、选文件

点「**选择 RAW 文件…**」，把那一组**全选**（Ctrl+A）。三个入口的照片混在同一个文件夹里没关系；拍废的不选进来即可，不必移走。

---

## 三、看软件说了什么

这是唯一需要你动脑的一步。软件自动配对，把问题分三类：**红色=不能用，要补拍**；**黄色=可用，但你该知道**；**灰色=找到几对，可以处理**。

![扫描结果](docs/images/03-dark-scan.png)

![PTC 的各种提示](docs/images/07-ptc-notes.png)

| 提示 | 怎么办 |
|---|---|
| 只有 1 张 / 有奇数张，xxx 落了单 | 这一档没成对。落单的那张会被单独退回，其余照常 |
| 光圈不一致 | 重拍这一对。**电子光圈每次收缩都有误差，优先手动光圈镜头** |
| 比黑电平高 xx DN | 漏光了，或根本不是黑场。检查机身盖 |
| 相隔 xx 分钟 | 温度可能变了。能重拍就重拍，不能的话这一档打个问号 |
| 快门比建议的慢 | 不拦你，但机身盖不密封，漏光按曝光时间累积 |
| 只有 N 个曝光级别 | 15 级以下拟合不稳，补到 25 级以上 |
| **没有一张过曝** | 见下 |

> **没有过曝帧的 PTC 曲线没有顶。**满阱测不到，拟合出来的「饱和点」只是「你最亮那张恰好是多少」——曲线看起来完全正常，拿到数据的人也发现不了。
>
> **一个通道过曝就够了**，绿色通道总是最先到顶。

---

## 四、处理

「中心裁切」保持默认（黑场和增益取中心 5900×3900，PTC 取中心 512×512），点「**开始处理**」，一对大约十秒。

并行度是逻辑核数的一半、最多 4，窗口右上角显示实际值。封顶是因为解包一帧要几百 MB，而这批活本来就卡在读盘。

![结果表](docs/images/04-dark-results.png)

**白底 = 实测值**，会写进文件；**灰底 = 推算值**，只显示不写入。

一眼看健康不健康：同一 ISO 下两个绿色通道应该几乎相同；`黑电平 A` 和 `B` 差不到 0.1；随 ISO 升高 `时域读噪` 应该单调变化，不该跳来跳去。

PTC 结果一行一个曝光级别：

![PTC 结果](docs/images/08-ptc-results.png)

---

## 五、保存并寄回

点「**保存 CSV…**」选个文件夹，软件把这一组的所有 CSV 打成一个 zip。**寄回这一个 zip 即可。**一整套黑场约 2 MB，邮件附件就能发。

![保存完成](docs/images/05-saved.png)

---

## 三个入口

| | 入口 3 · 黑场对 | 入口 2 · ISO 增益阶梯 | 入口 1 · PTC 平场对 |
|---|---|---|---|
| 拍什么 | 机身盖，每 ISO 两张 | 固定场景扫 ISO | 扫快门，每级两张 |
| 产出 | 黑电平、时域读噪、噪声谱 | 各档 ISO 的电平 | 完整噪声模型的原料 |
| 张数 | 40–56 | 约 20 | 50–60 |

软件这边三个入口谁先谁后都行——每组只记它自己量到的东西，不做跨组计算。PTC 表里不写黑电平，那是黑场组量到的，两份表寄回来拼起来是分析端的事。

**入口 2** 有两种拍法，软件自己认并在标签上标出来：**同快门配对**（快门在算式里被约掉，不吃标称值的准确度）和**自动快门扫 ISO**（快，但依赖 RAW 里记的标称快门时间）。漏了某一档不用重拍整组，补一张同快门的、和相邻已有 ISO 各一张就能接回去。

![增益阶梯](docs/images/06-gain-scan.png)

**入口 1** 同一快门可以拍不止一对（按文件名顺序两两配对，但这**不增加曝光级别**）；多个 ISO 可以一次丢进来，每个 ISO 各存一个 CSV。

---

## 常见问题

**会联网吗 / 会改我的 RAW 吗？** 都不会，全程本地、只读。处理到一半关掉也不留任何东西。

**「采集端不做任何修正」是什么意思？** 写进文件的每个数都是原始实测值：不除 √2、不做 Sheppard 修正、不减黑电平。要不要修正由拿到数据的人决定，这样以后换个算法还能重算。唯一不可逆的是坏点剪切（k=3.5），所以它的输入全记下来了：k 值、未剪切的原值、被剔除的像素数——不可撤销，但可审计。

**有什么不对劲怎么办？** 觉得奇怪、报了看不懂的错、或者你的机器有特殊情况（没有电子快门、ISO 不是整档、RAW 有损且关不掉）——**直接问，不要自己想办法绕过去。**绕过去的数据看起来是好的，用起来是错的，事后还查不出来。
