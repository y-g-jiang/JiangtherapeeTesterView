{
  "name": "jptc-collect",
  "private": true,
  "version": "0.1.0",
  "description": "Contributor-side capture and analysis tool for PTC, ISO gain and dark-frame spectra.",
  "type": "module",
  "scripts": {
    "build:native": "node-gyp rebuild -C native",
    "configure:native": "node-gyp configure -C native",
    "probe": "node scripts/probe-raw.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp": "^11.0.0",
    "vitest": "^2.1.9"
  }
}
